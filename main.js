/*$(document).ready(function(){
/*  var aboveHeight = 0;
  $(window).scroll(function(){
    if ($(window).scrollTop() > aboveHeight){
      document.getElementById("header_bar").style.position = "fixed";
      document.getElementById("header_bar").style.top = 0;
    }
    else {
      document.getElementById("header_bar").style.position = "relative";
    }
  });

  $(function(){
    $('#header_bar').data('size','big');
});

$(window).scroll(function(){
    if($(document).scrollTop() > 0)
    {
        if($('#header_bar').data('size') == 'big')
        {
            $('#header_bar').data('size','small');
            $('#header_bar').stop().animate({
                height:'20px'
            },600);
        }
    }
    else
    {
        if($('#header_bar').data('size') == 'small')
        {
            $('#header_bar').data('size','big');
            $('#header_bar').stop().animate({
                height:'80px'
            },600);
        }
    }
});
});*/
